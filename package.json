{
  "name": "just-food-delivery",
  "version": "1.0.0",
  "description": "Full-stack food delivery app — React frontend + Express/MongoDB backend",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}
